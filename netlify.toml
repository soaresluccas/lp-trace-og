[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "npm run dev:client"
  port = 5000
  targetPort = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
